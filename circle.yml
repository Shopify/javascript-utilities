machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

dependencies:
  override:
    - yarn --ignore-engines
  cache_directories:
    - ~/.cache/yarn

test:
  pre:
    - yarn run check
  override:
    - yarn run test
